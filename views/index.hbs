<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
  </head>
  <body>
    <h1>Discussion forums</h1>
    {{#if ! userLoggedin}}
      <p>
        <a href="/login">Sign in</a>
        |
        <a href="/register"></a></p>
    {{/if}}
    {{#else}}
      <h2>Actions</h2>
      {{#each actions}}
        action.name
      {{/each}}
    {{/else}}
    <h2>Recent discussions</h2>
    {{#each discussion}}
      {{Link if logged in}}
    {{/each}}
  </body>
</html>